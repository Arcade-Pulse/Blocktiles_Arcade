using UnityEngine;
using UnityEngine.UI;

public class LoadBanner : MonoBehaviour
{
    // [SerializeField] private Button cashoutBackButton;

    private void Start()
    {
        // cashoutBackButton.onClick.AddListener(() => BannerViewController.Instance.LoadAd());
        //BannerViewController.Instance.LoadAd();
    }
}
